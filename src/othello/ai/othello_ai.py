from src.othello.board import Board
import time


class OthelloAI:
    def choose_move(board: Board):
        nodes = 0
        cutoffs = 0
        start = time.time()
        return

    def _negamax(board, )
